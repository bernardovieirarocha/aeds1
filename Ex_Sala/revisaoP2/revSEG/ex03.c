// Essa atividade não faz nenhum sentido.

#include <stdio.h>
#include <stdlib.h>

int main(void) {
  int tam;
  // scanf("%d", &tam);

  // char *string2 = NULL;
  // char *string3 = NULL;

  // string2 = (char *)malloc(tam * sizeof(char));
  // string3 = (char *)malloc(tam * sizeof(char));

  // scanf("%s", string1);
  // scanf("%s", string2);

  return 0;
}